---
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import Contact from '../components/Contact.astro';
import Footer from '../components/Footer.astro';
---
<Base
  title="The Focused Acceleration Model — Rupture"
  description="Rupture's framework for building a real edge with AI: a clear order for building an AI capability, and a way to check whether work already underway is being built on the right foundations."
>
  <div class="wrap">
    <Header active="model" />

    <main class="fam-page">
      <div class="fam-root">

        <!-- ===== INTERACTIVE FIGURE ===== -->
        <figure class="fam-figure">
          <div class="fam-figure-title" id="famFigTitle" data-show="false" aria-hidden="true">
            Focused Acceleration<sup>&trade;</sup><span class="fam-stop">.</span>
          </div>

          <!-- Intro sits in the open top-left space of the chart on desktop,
               and stacks above it on mobile. -->
          <section class="fam-head">
            <h1 class="fam-title">Focused Acceleration<sup>&trade;</sup><span class="fam-stop">.</span></h1>
            <p class="fam-lede">
              This isn&rsquo;t about doing AI. It&rsquo;s about designing competitive advantage and using AI to
              reach it faster. Our model helps you adopt AI with the discipline, foresight and visibility
              that investors and boards can take confidence in.
            </p>
          </section>

          <div class="fam-stage">
            <svg class="fam-svg" id="famSvg" viewBox="0 0 1120 630" role="group"
                 aria-label="The Focused Acceleration Model: five stages climbing from a converged-market baseline to competitive daylight."
                 data-selection="false">

              <!-- Baseline -->
              <line class="fam-baseline" x1="60" y1="540" x2="1060" y2="540"></line>
              <text class="fam-axis-label" x="60" y="596">THE CONVERGED MARKET &mdash; EVERYONE OPTIMISING THE SAME WAY</text>

              <!-- Climbing line -->
              <path class="fam-climb" id="famClimb"></path>
              <polygon class="fam-arrowhead" id="famArrow"></polygon>

              <!-- Iterate loop between stage 04 and 05 -->
              <g id="famLoop" aria-hidden="true">
                <path class="fam-loop-path" id="famLoopPath"></path>
                <polygon class="fam-loop-arrow" id="famLoopArrow"></polygon>
                <text class="fam-loop-text" id="famLoopText1"></text>
                <text class="fam-loop-text" id="famLoopText2"></text>
              </g>

              <!-- Daylight drop near stage 05 -->
              <g id="famDaylight" aria-hidden="true">
                <line class="fam-daylight-line" id="famDaylightLine"></line>
                <text class="fam-daylight-text" id="famDaylightText">DAYLIGHT</text>
              </g>

              <!-- Nodes injected by JS -->
              <g id="famNodes"></g>
            </svg>
            <p class="fam-axis-mobile" aria-hidden="true">The converged market &mdash; everyone optimising the same way</p>
          </div>

          <!-- Detail panel -->
          <div class="fam-panel" id="famPanel" data-accent="false" data-coral="false" aria-live="polite">
            <div class="fam-panel-num" id="famPanelNum">01</div>
            <div class="fam-panel-body">
              <h2 class="fam-panel-name" id="famPanelName">Define the destination</h2>
              <p class="fam-panel-desc" id="famPanelDesc"></p>
            </div>
            <div class="fam-panel-ctrl">
              <span class="fam-panel-pos" id="famPanelPos">01 / 05</span>
              <div class="fam-panel-nav">
                <button type="button" class="fam-nav-btn" id="famPrev" aria-label="Previous stage">&larr;</button>
                <button type="button" class="fam-nav-btn" id="famNext" aria-label="Next stage">&rarr;</button>
              </div>
            </div>
          </div>

          <figcaption class="fam-figcap">
            The Focused Acceleration Model gives a clear order for building an AI capability, and a way to
            check whether work already underway is being built on the right foundations.
          </figcaption>
        </figure>

        <p class="fam-colophon">Focused Acceleration Model&trade; &nbsp;&middot;&nbsp; &copy; Rupture &mdash; This Is Rupture Ltd</p>

      </div>
    </main>

    <Contact />
    <Footer />
  </div>
</Base>

<style is:global>
  /* ------------------------------------------------------------------
     The Focused Acceleration Model — interactive figure.
     Themed entirely with Rupture design tokens (tokens.css), so it
     inherits the site's paper, ink, mint, coral and type automatically.
     All selectors are scoped under .fam-root to avoid leaking.
     ------------------------------------------------------------------ */

  .fam-page { min-width: 0; }
  /* No extra top padding: the .wrap gutter gap already sets the spacing below
     the nav, matching every other page. Blocks are spaced by the same gutter. */
  .fam-root { padding: 0; display: flex; flex-direction: column; gap: var(--gutter); }

  /* ---- Intro: overlaid in the open top-left space of the chart on desktop,
         so the copy and the figure share one bordered container. ---- */
  .fam-root .fam-head {
    position: absolute; z-index: 2;
    top: clamp(18px, 3vw, 40px); left: clamp(18px, 3vw, 44px);
    max-width: 46ch;
  }

  .fam-root .fam-title {
    font-family: var(--font-serif);
    font-weight: 700;
    font-size: clamp(1.9rem, 3.2vw, 2.7rem);
    line-height: 1.0; letter-spacing: -0.02em;
    margin: 0 0 16px; max-width: 18ch;
  }
  .fam-root .fam-title sup { font-size: 0.34em; top: -1em; font-weight: 400; }
  .fam-root .fam-title .fam-stop { color: var(--coral); }

  .fam-root .fam-lede {
    font-family: var(--font-serif); font-weight: 400;
    font-size: clamp(1.02rem, 1.4vw, 1.18rem); line-height: 1.45;
    max-width: 44ch; margin: 0;
  }

  /* ---- Figure frame ---- */
  /* No scroll-snap on desktop: wheel/trackpad snapping reads as hijacked input,
     and the figure exceeds shorter viewports, where CSS snap misbehaves anyway. */
  .fam-root .fam-figure { position: relative; margin: 0; border: var(--hairline); background: var(--paper); }

  /* In-figure title: hidden on desktop; on mobile it fades in once the page
     headline has scrolled away, so the framework stays named while in the chart. */
  .fam-root .fam-figure-title { display: none; }
  .fam-root .fam-stage { padding: clamp(20px, 3vw, 40px) clamp(16px, 3vw, 40px) 8px; }
  .fam-root .fam-svg { width: 100%; height: auto; display: block; overflow: visible; }

  .fam-root .fam-svg text { fill: var(--ink); }
  .fam-root .fam-axis-label {
    font-family: var(--font-mono); font-size: 13px; letter-spacing: 0.14em;
    fill: var(--fg-muted); text-transform: uppercase;
  }
  .fam-root .fam-axis-mobile { display: none; }

  /* Entrance animation keyframes */
  @keyframes fam-draw { to { stroke-dashoffset: 0; } }
  @keyframes fam-fade { from { opacity: 0; } to { opacity: 1; } }

  /* Climbing line */
  .fam-root .fam-climb { fill: none; stroke: var(--ink); stroke-width: 2.5; stroke-linecap: round; stroke-linejoin: round; }
  .fam-root .fam-arrowhead { fill: var(--coral); }
  .fam-root[data-anim="on"] .fam-arrowhead { opacity: 0; animation: fam-fade 0.4s 1.65s forwards; }

  /* Baseline */
  .fam-root .fam-baseline { stroke: var(--fg-faint); stroke-width: 1.5; }

  /* Daylight drop */
  .fam-root .fam-daylight-line { stroke: var(--mint-ink); stroke-width: 1.5; stroke-dasharray: 5 5; }
  .fam-root .fam-daylight-arrow { fill: var(--mint-ink); }
  .fam-root .fam-daylight-text {
    font-family: var(--font-mono); font-size: 13px; letter-spacing: 0.16em;
    fill: var(--mint-ink); text-transform: uppercase;
  }

  /* Performance-feedback loop — bright coral, revealed only when stage 05 is selected */
  .fam-root #famLoop { opacity: 0; transition: opacity 0.24s var(--ease); }
  .fam-root .fam-loop-path { fill: none; stroke: var(--coral); stroke-width: 2; stroke-linecap: round; }
  .fam-root .fam-loop-arrow { fill: var(--coral); }
  .fam-root .fam-loop-text { font-family: var(--font-mono); font-size: 12px; letter-spacing: 0.16em; fill: var(--coral); }

  /* Nodes.
     Inactive ("knocked back") nodes keep an OPAQUE paper fill so the climbing
     curve is always fully hidden behind the circle — never showing through.
     Dimming is expressed through stroke-opacity and text opacity, not group
     opacity, which is what previously let the curve bleed through. */
  .fam-root .fam-node { cursor: pointer; outline: none; }
  .fam-root[data-anim="on"] .fam-node { opacity: 0; animation: fam-fade 0.4s var(--fam-delay, 0s) forwards; }
  .fam-root .fam-node-dot {
    fill: var(--paper); stroke: var(--ink); stroke-width: 2.5; stroke-opacity: 0.5;
    transition: fill 0.2s var(--ease, ease), stroke 0.2s var(--ease, ease), stroke-opacity 0.2s var(--ease, ease), r 0.2s var(--ease, ease);
  }
  .fam-root .fam-node[data-accent="true"] .fam-node-dot { stroke: var(--mint); }
  .fam-root .fam-node[data-coral="true"]  .fam-node-dot { stroke: var(--coral); }

  .fam-root .fam-node-num {
    font-family: var(--font-mono); font-size: 16px; font-weight: 600; letter-spacing: 0.02em;
    fill: var(--ink); opacity: 0.55; dominant-baseline: central;
    transition: fill 0.2s var(--ease, ease), opacity 0.2s var(--ease, ease);
  }
  .fam-root .fam-node[data-accent="true"] .fam-node-num { fill: var(--mint-ink); }
  .fam-root .fam-node[data-coral="true"]  .fam-node-num { fill: var(--coral); }

  .fam-root .fam-node-name {
    font-family: var(--font-sans); font-size: 14px; font-weight: 600;
    fill: var(--ink); opacity: 0.5;
    transition: fill 0.2s var(--ease, ease), opacity 0.2s var(--ease, ease);
  }

  /* Focus ring (node radius is set per layout in JS, so we don't override it here) */
  .fam-root .fam-node:focus-visible .fam-node-dot { stroke: var(--mint-ink); }

  /* Active (selected) — bright ink / mint / coral fills */
  .fam-root .fam-node[data-active="true"] .fam-node-dot { fill: var(--ink); stroke: var(--ink); stroke-opacity: 1; }
  .fam-root .fam-node[data-active="true"][data-accent="true"] .fam-node-dot { fill: var(--mint); stroke: var(--mint); }
  .fam-root .fam-node[data-active="true"][data-coral="true"]  .fam-node-dot { fill: var(--coral); stroke: var(--coral); }
  .fam-root .fam-node[data-active="true"] .fam-node-num { fill: var(--paper); opacity: 1; }
  .fam-root .fam-node[data-active="true"][data-accent="true"] .fam-node-num { fill: var(--ink); }
  .fam-root .fam-node[data-active="true"] .fam-node-name { opacity: 1; }

  /* ---- Detail panel ---- */
  .fam-root .fam-panel {
    margin: 0; border-top: var(--hairline);
    padding: 24px clamp(16px, 3vw, 30px);
    min-height: 172px;
    display: grid; grid-template-columns: auto 1fr auto; gap: 20px 24px; align-items: start;
  }
  .fam-root .fam-panel-num {
    font-family: var(--font-mono); font-size: clamp(30px, 5vw, 44px); font-weight: 600;
    color: var(--fg-faint); line-height: 1;
  }
  .fam-root .fam-panel[data-accent="true"] .fam-panel-num { color: var(--mint); }
  .fam-root .fam-panel[data-coral="true"]  .fam-panel-num { color: var(--coral); }
  .fam-root .fam-panel-name {
    font-family: var(--font-sans); font-weight: 700; font-size: clamp(1.15rem, 2.2vw, 1.4rem);
    line-height: 1.15; margin: 0 0 10px;
  }
  .fam-root .fam-panel-desc { font-size: clamp(1rem, 1.6vw, 1.05rem); line-height: 1.55; max-width: 66ch; margin: 0; }
  .fam-root .fam-panel-ctrl {
    display: flex; flex-direction: column; align-items: flex-end; gap: 12px; justify-self: end;
  }
  .fam-root .fam-panel-pos {
    font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.1em; color: var(--fg-muted);
  }
  .fam-root .fam-panel-nav { display: flex; gap: 8px; }
  .fam-root .fam-nav-btn {
    width: 44px; height: 44px; border: 1.5px solid var(--ink); border-radius: 6px;
    background: var(--paper); cursor: pointer; font-family: var(--font-mono); font-size: 15px;
    color: var(--ink); transition: background 0.15s var(--ease), opacity 0.2s var(--ease);
  }
  .fam-root .fam-nav-btn:hover { background: var(--paper-2); }
  .fam-root .fam-nav-btn:disabled { opacity: 0.32; cursor: default; }
  .fam-root .fam-colophon {
    font-family: var(--font-mono); font-size: 11px; letter-spacing: 0.08em; color: var(--fg-muted);
    margin: 0;
  }

  /* ---- Figcaption ---- */
  .fam-root .fam-figcap {
    font-family: var(--font-mono); font-size: 10px; letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--fg-muted); background: var(--paper);
    border-top: var(--hairline-faint); padding: 12px 14px; line-height: 1.5;
  }

  /* ---- Responsive: on mobile the nodes grow, labels drop away, and the
         detail content sits underneath the chart ---- */
  /* Mobile: the camera viewport over the desktop curve, with the copy stacked
     full-width beneath. The SVG is clipped so off-camera artwork never bleeds
     out of the frame, and the axis caption becomes an HTML strip that wraps. */
  @media (max-width: 900px) {
    /* Mobile-only proximity snap: the figure is a self-contained unit (chart,
       stepper, copy), so landing near it seats it cleanly at the top of the
       viewport — while scrolling past stays free. Touch momentum makes
       proximity feel natural here in a way it doesn't with a mouse wheel. */
    html { scroll-snap-type: y proximity; }
    .fam-root .fam-figure { scroll-snap-align: start; scroll-margin-top: 10px; }

    /* Suppress double-tap zoom inside the figure (rapid taps on nodes/arrows
       were triggering browser zoom). Pinch-zoom on the page is unaffected. */
    .fam-root .fam-figure, .fam-root .fam-figure * { touch-action: manipulation; }

    .fam-root .fam-figure-title {
      display: block;
      font-family: var(--font-serif); font-weight: 700; font-size: 1.15rem;
      line-height: 1.2; letter-spacing: -0.01em;
      padding: 14px 16px 0;
      opacity: 0; transform: translateY(-4px);
      transition: opacity 0.3s var(--ease), transform 0.3s var(--ease);
    }
    .fam-root .fam-figure-title[data-show="true"] { opacity: 1; transform: none; }
    .fam-root .fam-figure-title sup { font-size: 0.5em; font-weight: 400; }
    .fam-root .fam-figure-title .fam-stop { color: var(--coral); }

    /* Intro returns to normal flow above the chart — no room to overlay. */
    .fam-root .fam-head {
      position: static; max-width: none;
      padding: 26px clamp(16px, 4vw, 22px) 4px;
    }
    .fam-root .fam-title { font-size: 1.85rem; margin-bottom: 12px; }
    .fam-root .fam-lede { font-size: 1.05rem; }

    .fam-root .fam-stage { padding: clamp(14px, 3.5vw, 20px) clamp(12px, 3vw, 18px) 0; }
    .fam-root .fam-svg { overflow: hidden; touch-action: pan-y; cursor: grab; }
    .fam-root .fam-svg:active { cursor: grabbing; }
    .fam-root .fam-axis-label { display: none; }
    .fam-root .fam-axis-mobile {
      display: block; margin: 10px 2px 12px;
      font-family: var(--font-mono); font-size: 10.5px; letter-spacing: 0.12em;
      text-transform: uppercase; color: var(--fg-muted); line-height: 1.5;
    }

    .fam-root .fam-panel { display: flex; flex-direction: column; min-height: 0; padding: 16px clamp(16px, 4vw, 22px) 20px; }
    .fam-root .fam-panel-num { font-size: 30px; margin-bottom: 4px; }
    .fam-root .fam-panel-name { font-size: 1.35rem; margin-bottom: 8px; }
    .fam-root .fam-panel-desc { font-size: 1.1rem; line-height: 1.6; max-width: none; }
    /* Arrows above the copy: a fixed, thumb-friendly position that doesn't
       move as the description length changes between stages. */
    .fam-root .fam-panel-ctrl {
      order: -1; flex-direction: row; align-items: center; justify-content: space-between;
      border-top: 0; border-bottom: var(--hairline-faint);
      padding: 0 0 14px; margin: 0 0 16px;
    }
    .fam-root .fam-nav-btn { width: 52px; height: 52px; font-size: 18px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .fam-root .fam-climb, .fam-root .fam-node-dot, .fam-root .fam-node-name, .fam-root .fam-node-num { transition: none !important; }
    .fam-root[data-anim="on"] .fam-node,
    .fam-root[data-anim="on"] .fam-arrowhead { animation: none !important; opacity: 1 !important; }
    .fam-root #famLoop .fam-loop-path { animation: none !important; }
  }
</style>

<script>
  // The Focused Acceleration Model — figure logic.
  // Pure attribute toggling; all colour lives in CSS tokens above.
  const STAGES = [
    { num: "01", line: ["Define the", "destination"], name: "Define the destination",
      desc: "Leadership begins by defining what the organisation intends to become—not as a vision exercise, but as the strategic ambition that shapes every decision that follows. What company are you trying to build that doesn't exist today? The destination does not determine the competitive advantage, but it defines the space in which that advantage must be found and provides the reference point for every subsequent decision. It also provides a basis for assessing whether existing initiatives are aligned to a common strategic direction.",
      accent: false, coral: false },
    { num: "02", line: ["Name the", "frame"], name: "Name the frame",
      desc: "Every market operates within a set of accepted beliefs, assumptions and perceived constraints. Before deciding how to compete differently, those assumptions are made explicit—starting with the organisation's own thinking—to identify the underlying market assumptions through which enduring competitive advantage can be created or strengthened. It also provides a basis for assessing whether existing decisions are reinforcing the current market frame or challenging it.",
      accent: false, coral: false },
    { num: "03", line: ["Design the", "advantage"], name: "Design the advantage",
      desc: "Not every assumption is worth challenging. Potential opportunities are assessed for strategic leverage, commercial impact, defensibility and organisational fit before a clearly defined domain of competitive advantage is selected. It also provides a basis for assessing whether existing initiatives are strengthening the chosen domain of advantage or dispersing investment across competing priorities.",
      accent: true, coral: false },
    { num: "04", line: ["Build the", "capability"], name: "Build the capability",
      desc: "The capabilities, operating model, governance, technology and organisational changes required to deliver the chosen domain of competitive advantage are defined and sequenced into a practical transformation roadmap. It also provides a basis for assessing whether current investments are building the capabilities required to sustain that position or responding primarily to short-term priorities.",
      accent: false, coral: false },
    { num: "05", line: ["Accelerate the", "advantage"], name: "Accelerate the advantage",
      desc: "AI is applied to strengthen the capabilities that underpin the chosen domain of competitive advantage, increasing the organisation's ability to outperform competitors over time. Performance is continuously measured to validate progress, refine implementation and strengthen competitive separation. It also provides a basis for assessing whether existing AI investments are creating measurable strategic advantage or delivering operational improvement alone.",
      accent: false, coral: true }
  ];

  const NS = "http://www.w3.org/2000/svg";
  const N = STAGES.length;

  // Two layouts. Desktop is a wide, shallow climb. Mobile is a tall portrait
  // climb so phones get real vertical space, big legible nodes (no labels) and
  // the detail copy full-width underneath — not a shrunk-down desktop chart.
  const DESKTOP = {
    viewBox: "0 0 1120 630",
    base: [60, 540, 1060, 540], axis: [60, 596], axisFs: 13,
    nodes: [[150, 540], [360, 480], [565, 390], [770, 275], [955, 140]],
    r: 22, numFs: 16, labels: true, labelGap: 60, labelFs: 14, labelLead: 17,
    climb: "M 150 540 C 220 540 290.8 505 360 480 C 429.2 455 496.7 424.2 565 390 C 633.3 355.8 705 316.7 770 275 C 835 233.3 910 174.5 955 140 C 1000 105.5 1020 84 1032 74",
    loop: { d: "M 972 127 A 45 45 0 1 1 938 153", arrow: "938,153 939.1,165.5 928.1,160.7", t: [979, 245, 261], anchor: "middle", fs: 12 },
    day: { line: [1058, 102, 1058, 540], arrow: "1058,88 1052.5,102 1063.5,102", tx: 1066, ty: 318, fs: 13 }
  };
  // Mobile reuses the exact desktop curve, but with larger nodes and no labels,
  // and is viewed through a camera: a window over the graph that pans to centre
  // the active stage. Same geometry, same semantics — just seen a slice at a time.
  const MOBILE = {
    ...DESKTOP,
    r: 40, numFs: 30, labels: false,
    axisFs: 16,
    loop: { ...DESKTOP.loop, fs: 16 },
    day: { ...DESKTOP.day, fs: 16 },
    camera: true,
    cam: { w: 470, h: 400, x0: 40, x1: 650, y0: 40, y1: 230 }
  };

  const svg    = document.getElementById("famSvg");
  const nodesG = document.getElementById("famNodes");
  const climb  = document.getElementById("famClimb") as unknown as SVGPathElement;
  const arrow  = document.getElementById("famArrow");
  const baseline = document.querySelector(".fam-baseline");
  const axisText = document.querySelector(".fam-axis-label");
  const loopG    = document.getElementById("famLoop");
  const loopPath = document.getElementById("famLoopPath") as unknown as SVGPathElement | null;
  const loopArrow = document.getElementById("famLoopArrow");
  const loopT1   = document.getElementById("famLoopText1");
  const loopT2   = document.getElementById("famLoopText2");
  const dayG     = document.getElementById("famDaylight");
  const dayLine  = document.getElementById("famDaylightLine");
  const dayText  = document.getElementById("famDaylightText");
  const dayArrow = document.createElementNS(NS, "polygon");
  dayArrow.setAttribute("class", "fam-daylight-arrow");
  dayG?.insertBefore(dayArrow, dayG.firstChild);

  if (svg && nodesG && climb && arrow) {
    const panel = document.getElementById("famPanel");
    const pNum  = document.getElementById("famPanelNum");
    const pName = document.getElementById("famPanelName");
    const pDesc = document.getElementById("famPanelDesc");
    const pPos  = document.getElementById("famPanelPos");
    const prevBtn = document.getElementById("famPrev") as HTMLButtonElement | null;
    const nextBtn = document.getElementById("famNext") as HTMLButtonElement | null;
    const root = document.querySelector(".fam-root") as HTMLElement | null;
    const reduceMotion = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    const mq = window.matchMedia("(max-width: 900px)");

    let nodeEls: SVGGElement[] = [];
    let loopLen = 0;
    let current = 0;

    // Camera (mobile only): a window over the graph that pans in both axes to
    // frame the active stage — following the climb up as well as across.
    let camActive = false;
    let cam: any = null;
    let camNodes: number[][] = [];
    let camX = 0, camY = 0;

    function setViewBox(x: number, y: number) {
      svg!.setAttribute("viewBox", x + " " + y + " " + cam.w + " " + cam.h);
    }

    function camTarget(i: number) {
      const tx = Math.max(cam.x0, Math.min(cam.x1, camNodes[i][0] - cam.w / 2));
      const ty = Math.max(cam.y0, Math.min(cam.y1, camNodes[i][1] - cam.h / 2));
      return [tx, ty];
    }

    function panTo(i: number, animate: boolean) {
      if (!camActive || !cam) return;
      const [tx, ty] = camTarget(i);
      if (!animate || reduceMotion) { camX = tx; camY = ty; setViewBox(camX, camY); return; }
      const fx = camX, fy = camY, dur = 480;
      let start: number | null = null;
      function step(ts: number) {
        if (start === null) start = ts;
        const p = Math.min((ts - start) / dur, 1);
        const e = 1 - Math.pow(1 - p, 3);
        camX = fx + (tx - fx) * e;
        camY = fy + (ty - fy) * e;
        setViewBox(camX, camY);
        if (p < 1) requestAnimationFrame(step);
      }
      requestAnimationFrame(step);
    }

    function arrowPoly(tx: number, ty: number, ang: number, L: number, w: number) {
      const bx = tx - L * Math.cos(ang), by = ty - L * Math.sin(ang);
      return tx + "," + ty + " " +
        (bx - w * Math.sin(ang)) + "," + (by + w * Math.cos(ang)) + " " +
        (bx + w * Math.sin(ang)) + "," + (by - w * Math.cos(ang));
    }

    // Reveal (or hide) the feedback loop for stage 05.
    function setLoop(on: boolean, animate: boolean) {
      if (loopG) loopG.style.opacity = on ? "1" : "0";
      if (!on || !loopPath) return;
      if (!animate || reduceMotion) { loopPath.style.strokeDashoffset = "0"; return; }
      loopPath.style.strokeDashoffset = String(loopLen);
      loopPath.style.animation = "none";
      loopArrow && (loopArrow.style.animation = "none");
      loopT1 && (loopT1.style.animation = "none");
      loopT2 && (loopT2.style.animation = "none");
      void loopPath.getBoundingClientRect();
      loopPath.style.animation = "fam-draw 0.7s var(--ease) forwards";
      loopArrow && (loopArrow.style.animation = "fam-fade 0.4s 0.5s backwards");
      loopT1 && (loopT1.style.animation = "fam-fade 0.4s 0.55s backwards");
      loopT2 && (loopT2.style.animation = "fam-fade 0.4s 0.55s backwards");
    }

    function focusNode(i: number) {
      const n = nodeEls.length;
      nodeEls[((i % n) + n) % n].focus();
    }

    // Update the active node, the detail copy and the loop for the current stage.
    function refresh() {
      const s = STAGES[current];
      svg!.setAttribute("data-selection", "true");
      nodeEls.forEach((n, j) => n.setAttribute("data-active", j === current ? "true" : "false"));
      if (pNum)  pNum.textContent = s.num;
      if (pName) pName.textContent = s.name;
      if (pDesc) pDesc.textContent = s.desc;
      if (pPos)  pPos.textContent = s.num + " / 0" + N;
      panel?.setAttribute("data-accent", s.accent ? "true" : "false");
      panel?.setAttribute("data-coral", s.coral ? "true" : "false");
    }

    // Wrap-around so the stepper runs continuously in both directions (05 → 01, 01 → 05).
    function select(i: number) {
      current = ((i % N) + N) % N;
      refresh();
      setLoop(current === N - 1, true);
      panTo(current, true);
    }

    // (Re)build the whole figure for a given layout, preserving the selection.
    function build(cfg: any) {
      baseline?.setAttribute("x1", cfg.base[0]); baseline?.setAttribute("y1", cfg.base[1]);
      baseline?.setAttribute("x2", cfg.base[2]); baseline?.setAttribute("y2", cfg.base[3]);
      if (axisText) { axisText.setAttribute("x", cfg.axis[0]); axisText.setAttribute("y", cfg.axis[1]); (axisText as unknown as SVGElement).style.fontSize = cfg.axisFs + "px"; }

      climb.style.animation = "none";
      climb.style.strokeDasharray = "";
      climb.style.strokeDashoffset = "";
      climb.setAttribute("d", cfg.climb);
      const cl = climb.getTotalLength();
      const p2 = climb.getPointAtLength(cl), p1 = climb.getPointAtLength(Math.max(0, cl - 14));
      arrow.setAttribute("points", arrowPoly(p2.x, p2.y, Math.atan2(p2.y - p1.y, p2.x - p1.x), 16, 7.5));

      loopPath?.setAttribute("d", cfg.loop.d);
      loopArrow?.setAttribute("points", cfg.loop.arrow);
      [[loopT1, cfg.loop.t[1], "PERFORMANCE"], [loopT2, cfg.loop.t[2], "FEEDBACK"]].forEach(([el, y, txt]: any) => {
        if (!el) return;
        el.setAttribute("x", cfg.loop.t[0]); el.setAttribute("y", y); el.setAttribute("text-anchor", cfg.loop.anchor);
        el.style.fontSize = cfg.loop.fs + "px"; el.textContent = txt;
      });
      if (loopPath) { loopLen = loopPath.getTotalLength(); loopPath.style.strokeDasharray = String(loopLen); loopPath.style.strokeDashoffset = String(loopLen); }

      dayLine?.setAttribute("x1", cfg.day.line[0]); dayLine?.setAttribute("y1", cfg.day.line[1]);
      dayLine?.setAttribute("x2", cfg.day.line[2]); dayLine?.setAttribute("y2", cfg.day.line[3]);
      dayArrow.setAttribute("points", cfg.day.arrow);
      if (dayText) { dayText.setAttribute("x", cfg.day.tx); dayText.setAttribute("y", cfg.day.ty); dayText.setAttribute("transform", "rotate(90 " + cfg.day.tx + " " + cfg.day.ty + ")"); dayText.setAttribute("text-anchor", "middle"); (dayText as unknown as SVGElement).style.fontSize = cfg.day.fs + "px"; }

      nodesG!.textContent = "";
      nodeEls = [];
      STAGES.forEach((s, i) => {
        const [nx, ny] = cfg.nodes[i];
        const g = document.createElementNS(NS, "g");
        g.setAttribute("class", "fam-node");
        g.setAttribute("tabindex", "0");
        g.setAttribute("role", "button");
        g.setAttribute("aria-label", "Stage " + s.num + ": " + s.name + ".");
        g.setAttribute("data-accent", s.accent ? "true" : "false");
        if (s.coral) g.setAttribute("data-coral", "true");
        g.setAttribute("data-active", "false");
        (g as unknown as SVGGElement).style.setProperty("--fam-delay", (0.4 + i * 0.3) + "s");

        const dot = document.createElementNS(NS, "circle");
        dot.setAttribute("class", "fam-node-dot");
        dot.setAttribute("cx", String(nx)); dot.setAttribute("cy", String(ny)); dot.setAttribute("r", String(cfg.r));
        g.appendChild(dot);

        const num = document.createElementNS(NS, "text");
        num.setAttribute("class", "fam-node-num");
        num.setAttribute("x", String(nx)); num.setAttribute("y", String(ny)); num.setAttribute("text-anchor", "middle");
        (num as unknown as SVGElement).style.fontSize = cfg.numFs + "px";
        num.textContent = s.num;
        g.appendChild(num);

        if (cfg.labels) {
          const name = document.createElementNS(NS, "text");
          name.setAttribute("class", "fam-node-name");
          name.setAttribute("x", String(nx)); name.setAttribute("y", String(ny - cfg.labelGap)); name.setAttribute("text-anchor", "middle");
          (name as unknown as SVGElement).style.fontSize = cfg.labelFs + "px";
          s.line.forEach((ln, k) => {
            const ts = document.createElementNS(NS, "tspan");
            ts.setAttribute("x", String(nx)); ts.setAttribute("dy", k === 0 ? "0" : String(cfg.labelLead));
            ts.textContent = ln;
            name.appendChild(ts);
          });
          g.appendChild(name);
        }

        nodesG!.appendChild(g);
        nodeEls.push(g);

        g.addEventListener("focus", () => select(i));
        g.addEventListener("click", () => select(i));
        g.addEventListener("keydown", (e) => {
          if (e.key === "Enter" || e.key === " ") { e.preventDefault(); select(i); }
          else if (e.key === "ArrowRight" || e.key === "ArrowDown") { e.preventDefault(); focusNode(i + 1); }
          else if (e.key === "ArrowLeft" || e.key === "ArrowUp") { e.preventDefault(); focusNode(i - 1); }
        });
      });

      // View: full frame on desktop, or the camera window on mobile.
      if (cfg.camera) {
        camActive = true;
        cam = cfg.cam;
        camNodes = cfg.nodes;
        panTo(current, false);
      } else {
        camActive = false;
        svg!.setAttribute("viewBox", cfg.viewBox);
      }

      refresh();
      setLoop(current === N - 1, false);
    }

    prevBtn?.addEventListener("click", () => select(current - 1));
    nextBtn?.addEventListener("click", () => select(current + 1));

    // Touch/drag panning (mobile): horizontal swipes pan the camera and snap to
    // the nearest stage on release. touch-action: pan-y in CSS keeps vertical
    // swipes scrolling the page, so the chart never traps the scroll.
    // Gesture axis-lock: the chart only pans once the finger is clearly moving
    // horizontally. A mostly-vertical swipe is handed to the page scroll
    // untouched, so scrolling never twitches the chart. Ambiguous diagonals
    // lean horizontal (the chart is the thing being explored); a definite
    // vertical swipe always wins for scrolling.
    let dragging = false, axis = "", dragMoved = false;
    let dragStartX = 0, dragStartY = 0, dragStartCam = 0;
    let lastPx = 0, lastT = 0, vel = 0; // px/ms, smoothed
    svg.addEventListener("pointerdown", (e: PointerEvent) => {
      if (!camActive) return;
      dragging = true; axis = ""; dragMoved = false;
      dragStartX = e.clientX; dragStartY = e.clientY; dragStartCam = camX;
      lastPx = e.clientX; lastT = performance.now(); vel = 0;
    });
    svg.addEventListener("pointermove", (e: PointerEvent) => {
      if (!dragging || !camActive) return;
      const dxPx = e.clientX - dragStartX;
      const dyPx = e.clientY - dragStartY;
      if (!axis) {
        // Definite vertical swipe → give the gesture to the page scroll.
        if (Math.abs(dyPx) > 14 && Math.abs(dyPx) > Math.abs(dxPx) * 1.3) { dragging = false; return; }
        // Horizontal (with a bias in its favour on diagonals) → pan the chart.
        if (Math.abs(dxPx) > 10 && Math.abs(dxPx) >= Math.abs(dyPx) * 0.9) {
          axis = "h";
          (svg as unknown as Element).setPointerCapture?.(e.pointerId);
        } else return;
      }
      dragMoved = true;
      const now = performance.now(), dt = now - lastT;
      if (dt > 0) vel = 0.75 * vel + 0.25 * ((e.clientX - lastPx) / dt);
      lastPx = e.clientX; lastT = now;
      const scale = cam.w / (svg as unknown as HTMLElement).clientWidth;
      camX = Math.max(cam.x0, Math.min(cam.x1, dragStartCam - dxPx * scale));
      setViewBox(camX, camY);
    });
    const endDrag = () => {
      if (!dragging) return;
      dragging = false;
      if (!dragMoved || !camActive) return;
      // Fling: a genuinely quick flick projects the camera further (05 → 01 in
      // one confident swipe); ordinary drags settle on the nearest stage.
      const scale = cam.w / (svg as unknown as HTMLElement).clientWidth;
      const momentum = Math.abs(vel) > 0.35 ? vel * 200 * scale : 0;
      const projected = Math.max(cam.x0, Math.min(cam.x1, camX - momentum));
      const centre = projected + cam.w / 2;
      let best = 0, bd = Infinity;
      camNodes.forEach((n, i) => { const d = Math.abs(n[0] - centre); if (d < bd) { bd = d; best = i; } });
      select(best);
    };
    svg.addEventListener("pointerup", endDrag);
    svg.addEventListener("pointercancel", endDrag);
    // A drag shouldn't also fire a node tap on release.
    svg.addEventListener("click", (e) => {
      if (dragMoved) { e.stopPropagation(); e.preventDefault(); dragMoved = false; }
    }, true);

    // In-figure title: fade in once the page headline scrolls out of view
    // (mobile-only via CSS), so the framework stays named inside the chart.
    const figTitle = document.getElementById("famFigTitle");
    const pageTitle = document.querySelector(".fam-title");
    if (figTitle && pageTitle && "IntersectionObserver" in window) {
      new IntersectionObserver((entries) => {
        figTitle.setAttribute("data-show", entries[0].isIntersecting ? "false" : "true");
      }).observe(pageTitle);
    }

    // Initial build + entrance animation, then rebuild on layout change.
    build(mq.matches ? MOBILE : DESKTOP);

    if (!reduceMotion && !document.hidden) {
      const len = climb.getTotalLength();
      climb.style.strokeDasharray = String(len);
      climb.style.strokeDashoffset = String(len);
      climb.style.animation = "fam-draw 1.6s 0.15s cubic-bezier(0.22, 1, 0.36, 1) forwards";
      root?.setAttribute("data-anim", "on");
      window.setTimeout(() => { climb.style.strokeDashoffset = "0"; root?.removeAttribute("data-anim"); }, 2600);
    }

    const onLayoutChange = () => build(mq.matches ? MOBILE : DESKTOP);
    if (mq.addEventListener) mq.addEventListener("change", onLayoutChange);
    else mq.addListener(onLayoutChange);
  }
</script>
